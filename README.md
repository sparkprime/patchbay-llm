# Developing

Use a virtualenv, typically .venv managed by uv, and activate it before running anything.

For quick turn arounds, run the ./format.sh and ./lint.sh scripts and also run pyright.

To run tests, run pytest.

